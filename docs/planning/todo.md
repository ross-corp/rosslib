# Features

Backlog of small tasks for nephewbot to pick off. Each item should be self-contained and implementable without external coordination. Items are ordered by priority — nephewbot picks the top unchecked item.


## BUGS

- [ ] Implement password reset and email verification backend: the webapp proxy routes exist (`POST /auth/forgot-password`, `POST /auth/reset-password`, `POST /auth/verify-email`, `POST /auth/resend-verification`) but the Go handler functions and backing DB tables (`password_reset_tokens`, `email_verification_tokens`) are entirely missing. The `email_verified` field exists on users. Requires: new migration for token tables, `api/internal/email` package for SMTP, four handler functions in `api/handlers/auth.go`, route registration in `api/main.go`. See `docs/planning/completed.md` "Password Reset" and "Email Verification" sections for full spec. **Large task — not suitable for nephewbot.**
- [ ] Implement set-operation and cross-user-compare backend: the webapp proxy routes exist (`POST /me/shelves/set-operation`, `POST /me/shelves/set-operation/save`, `POST /me/shelves/cross-user-compare`, `POST /me/shelves/cross-user-compare/save`) but no Go handler functions exist in `api/handlers/collections.go`. Requires: new `computed_collections` table migration, four handler functions, route registration. See `docs/planning/completed.md` "Computed Lists" sections for full spec. **Large task — not suitable for nephewbot.**

## Pending PRs

- [Fix search page popular section to match active tab](https://github.com/ross-corp/rosslib/pull/120) — show popular authors/people instead of always showing books when on Authors/People tabs
- [Fix: flatten simultaneous finish + rate into one feed event](https://github.com/ross-corp/rosslib/pull/119) — merge near-simultaneous finished_book + rated activities into a single "finished and rated" feed event
- [Fix library sidebar counts not updating on re-label](https://github.com/ross-corp/rosslib/pull/118) — optimistic count updates in sidebar when moving books between statuses or removing them
- [Add sort options to owner library view and tag/label endpoints](https://github.com/ross-corp/rosslib/pull/90) — sort dropdown in LibraryManager + sort param on tags/labels API endpoints
- [Add confirmation dialog when removing books from library](https://github.com/ross-corp/rosslib/pull/91) — reusable ConfirmDialog component, applied to shelf grid, library manager bulk remove, and shelf picker
- [Wire toast notifications into all user actions](https://github.com/ross-corp/rosslib/pull/92) — extend existing toast system to cover import, quick-add, bulk library ops, settings, export, block, and reading progress
- [Use BookCoverPlaceholder consistently across all book cover fallbacks](https://github.com/ross-corp/rosslib/pull/93) — replace plain div fallbacks with BookCoverPlaceholder in 18 files
- [Add reading pace calculation for currently-reading books](https://github.com/ross-corp/rosslib/pull/94) — show pages/day and estimated finish date below reading progress bar
- [Add re-read tracking with reading sessions](https://github.com/ross-corp/rosslib/pull/95) — reading_sessions collection, CRUD API endpoints, ReadingHistory component on book detail page
- [Add year-in-review summary page](https://github.com/ross-corp/rosslib/pull/96) — year-in-review API endpoint and page with stats, top books, genres, and month-by-month grid
- [Add periodic book_stats backfill every 24 hours](https://github.com/ross-corp/rosslib/pull/97) — bookstats package with BackfillAll + StartPoller goroutine running on startup and every 24h
- [Add feed filtering by activity type](https://github.com/ross-corp/rosslib/pull/98) — type query param on GET /me/feed and filter chips on feed page
- [Add friends reading this on book detail page](https://github.com/ross-corp/rosslib/pull/99) — GET /books/:workId/readers endpoint and avatar row on book detail page
- [Add follow suggestions on feed page](https://github.com/ross-corp/rosslib/pull/100) — GET /me/suggested-follows endpoint and FollowSuggestions component on feed page
- [Add profile banner image](https://github.com/ross-corp/rosslib/pull/101) — banner file field on users, POST /me/banner endpoint, banner display on profile page, upload in settings
- [Add favorite genres display on user profile](https://github.com/ross-corp/rosslib/pull/102) — top 5 genre chips derived from finished books' subjects, displayed below bio on profile page
- [Add trending books section to search landing page](https://github.com/ross-corp/rosslib/pull/103) — GET /books/trending endpoint and horizontal scrollable row on search page
- [Add recently viewed books (client-side)](https://github.com/ross-corp/rosslib/pull/104) — useRecentlyViewed hook with localStorage, RecordRecentlyViewed on book detail, recently viewed row on search page
- [Add saved searches](https://github.com/ross-corp/rosslib/pull/105) — saved_searches collection, CRUD API endpoints, SavedSearches component with chips on search page
- [Add review sorting options on book detail page](https://github.com/ross-corp/rosslib/pull/106) — sort query param on GET /books/:workId/reviews and sort dropdown on book detail page
- [Add 'more by this author' section on book detail page](https://github.com/ross-corp/rosslib/pull/107) — "More by {Author}" section with up to 6 book covers, reuses existing author works endpoint
- [Add book quotes/highlights](https://github.com/ross-corp/rosslib/pull/108) — book_quotes collection, CRUD API endpoints, BookQuoteList component on book detail page with public/private toggle
- [Add thread locking for moderators](https://github.com/ross-corp/rosslib/pull/109) — locked_at column on threads, lock/unlock endpoints, locked banner and mod toggle on frontend
- [Add review comments / discussion under reviews](https://github.com/ross-corp/rosslib/pull/110) — review_comments collection, GET/POST/DELETE endpoints, ReviewComments component on book detail page with notifications
- [Add label descriptions](https://github.com/ross-corp/rosslib/pull/111) — description column on collections, create/edit UI in library manager, render on visitor shelf detail page
- [Add LibraryThing CSV import](https://github.com/ross-corp/rosslib/pull/112) — LibraryThing TSV import with preview/commit endpoints, tab-delimited parsing, author name reversal, collections/tags as labels
- [Add API token generation for integrations](https://github.com/ross-corp/rosslib/pull/113) — api_tokens collection, CRUD endpoints, Bearer token auth in middleware, settings UI
- [Add loading skeleton components](https://github.com/ross-corp/rosslib/pull/114) — Skeleton base component, composed skeletons, loading.tsx files for feed/profile/book detail/search
- [Add keyboard shortcuts with help overlay](https://github.com/ross-corp/rosslib/pull/115) — useKeyboardShortcuts hook, shortcuts overlay modal, hint badge for logged-in users
- [Add dark mode with light/dark/system theme toggle](https://github.com/ross-corp/rosslib/pull/116) — theme infrastructure (CSS variables, data-theme attribute, FOUC prevention), theme toggle in nav, theme API endpoint, semantic color token conversion across ~45 files
- [Add empty state illustrations for zero-data pages](https://github.com/ross-corp/rosslib/pull/117) — reusable EmptyState component with consistent CTA links on feed, notifications, library, and label pages
- [Auto-predict label names from shelf naming patterns](https://github.com/ross-corp/rosslib/pull/121) — detect common prefix and year-based patterns in Goodreads/StoryGraph shelf names and auto-group them into labels during import

## UX Polish

- [ ] Show error toast when recommendation status update fails: in `webapp/src/app/recommendations/recommendation-list.tsx`, the `updateStatus()` function (line 36-52) calls `PATCH /api/me/recommendations/:id` and updates state on success, but does nothing on failure — the user gets no feedback if the accept/dismiss action failed. Add an `else` branch after `if (res.ok)` that calls `toast.error("Failed to update recommendation")`. Import `useToast` and add it to the component. This matches the error toast pattern used in FollowButton and BookFollowButton.
- [ ] Add `not-found.tsx` to genres detail page: the route `webapp/src/app/genres/[slug]/page.tsx` fetches genre data from the API but when a nonexistent genre slug is used, the page throws an error instead of showing a friendly "Genre not found" page. Create `webapp/src/app/genres/[slug]/not-found.tsx` following the pattern in `webapp/src/app/books/[workId]/not-found.tsx` — show "Genre not found" with a link back to `/genres`. In the page server component, call `notFound()` from `next/navigation` when the API returns 404.
- [ ] Add empty state to thread comments section when no comments exist: in `webapp/src/components/thread-comments.tsx`, when a thread has zero comments, the expanded comments section renders an empty area with just the "Add a comment" input. Add a `{comments.length === 0 && <p className="text-sm text-text-secondary py-4 text-center">No comments yet. Be the first to comment.</p>}` message above the input field when there are no comments. This matches the empty state pattern used in other list components.
- [ ] Show loading spinner during initial BookLinkList fetch: in `webapp/src/components/book-link-list.tsx`, the component fetches book links on mount but shows no loading indicator while the request is in flight. Add a `loading` state that's true initially, set to false after the fetch completes, and show a small spinner or "Loading related books..." text while `loading` is true. This matches the loading pattern used in `BookQuoteList` and `ReadingHistory` components.
- [ ] Add `new_follower` toggle to notification preferences UI: in `webapp/src/components/notification-preferences.tsx`, the toggle list includes preferences for new publications, threads, links, reviews, review likes, mentions, and recommendations, but does NOT include a toggle for `new_follower` notifications. The backend already has the `new_follower` field on `notification_preferences` (migration `1700000029`) and checks `ShouldNotify(app, userId, "new_follower")` when sending follow notifications. Add a toggle row labeled "New followers" between the existing "Recommendations" toggle and the "Mentions" toggle, using the same pattern as the other toggles.
- [ ] Add `loading.tsx` for settings sub-pages: the settings pages at `webapp/src/app/settings/` (import, export, api-tokens, blocked, feedback, follow-requests, followed-books, followed-authors, ghost-activity, goals, tags) have no `loading.tsx` files, so when navigating between settings sub-pages there is no visual loading state. Create `webapp/src/app/settings/loading.tsx` with a simple centered spinner or "Loading..." text, similar to the existing `webapp/src/app/feed/loading.tsx`. This provides immediate visual feedback during navigation between settings sections.

## API & Performance

- [ ] Batch vote counts and user-voted status into the book links query: in `api/handlers/links.go`, the `GetBookLinks()` function runs separate `SELECT COUNT(*)` and `SELECT id` queries for each link to get vote counts and the viewer's vote status (N+1 pattern). Rewrite to use a single query with `LEFT JOIN (SELECT book_link_id, COUNT(*) AS votes FROM book_link_votes GROUP BY book_link_id)` and a second `LEFT JOIN` for the viewer's vote, eliminating the per-link loops.
- [ ] Include `review_likes`, `review_comments`, `recommendations`, `computed_collections`, `feedback`, `reports`, and `api_tokens` in account data deletion: in `api/handlers/userdata.go`, both `DeleteAccount` (line 23-42) and `DeleteAllData` (line 95-113) are missing these collections from the deletion loop. Add them to the `userOwned` slice. For `recommendations`, also delete where `recipient = userId` (not just `user`). For `reports`, also delete where `reviewer = userId`. Note: `DeleteAllData` is also missing `recommendations` entirely (DeleteAccount has it but DeleteAllData does not). This is separate from the existing todo item for `blocks`/`saved_searches`/`reading_sessions`.
- [ ] Add `note` max length validation to CreateBookLink: in `api/handlers/links.go`, the `CreateBookLink()` handler accepts a `note` field but never validates its length. Add a check: if `len(note) > 2000`, return `400 { "error": "Note must be 2,000 characters or fewer" }`. This matches the max length pattern used for review comment body and quote text elsewhere in the codebase.
- [ ] Add `date_read` and `date_started` format validation to UpdateBook: in `api/handlers/userbooks.go`, the `UpdateBook()` handler (around line 153-160) accepts `date_read`, `date_dnf`, and `date_started` as string fields but sets them on the record without validating that they're valid dates. Add `time.Parse(time.RFC3339, dateStr)` validation for each date field when provided, returning `400 { "error": "Invalid date format for <field>" }` on failure. The `CreateSession` handler in `api/handlers/sessions.go` already validates dates this way — apply the same pattern to UpdateBook.
- [ ] Add date format validation to UpdateSession: in `api/handlers/sessions.go`, the `UpdateSession()` handler (around line 176-181) accepts `date_started` and `date_finished` fields and sets them on the record without validating that they're valid dates. Unlike `CreateSession()` (which validates dates around lines 94-107 with `time.Parse`), `UpdateSession` skips validation entirely. Add the same `time.Parse(time.RFC3339, dateStr)` validation for both date fields in `UpdateSession`, returning `400 { "error": "Invalid date format" }` on failure. Copy the validation pattern from `CreateSession`.
- [ ] Allow moderators to soft-delete threads and thread comments: in `api/handlers/threads.go`, the `DeleteThread()` handler (line 283) only allows the thread author to delete (`thread.GetString("user") != user.Id`). Add an `|| isModerator(e)` check so moderators can also soft-delete threads. Apply the same fix to `DeleteComment()` (line 376). This matches the pattern already used in `api/handlers/links.go` where `DELETE /links/:linkId` allows both the author and moderators to soft-delete. Use the existing `isModerator` helper that reads the JWT `is_moderator` claim.
- [ ] Add `thread_body` max length validation to AddComment: in `api/handlers/threads.go`, the `AddComment()` handler validates that the body is non-empty but does not enforce a maximum length. Add a check: if `len(data.Body) > 5000`, return `400 { "error": "Comment must be 5,000 characters or fewer" }`. The thread body itself is capped at 10,000 characters — comments should have a lower limit since they're supplementary content.
- [ ] Add `title` max length validation to CreateThread: in `api/handlers/threads.go`, the `CreateThread()` handler validates that the title is non-empty but does not enforce a max length beyond the database column's varchar(500). Add an explicit check: if `len(data.Title) > 500`, return `400 { "error": "Title must be 500 characters or fewer" }`. This provides a clear error message instead of a database constraint error.
- [ ] Batch blocked users lookup into a single query in GetBlockedUsers: in `api/handlers/blocks.go`, the `GetBlockedUsers()` function (line 143-162) runs a separate `app.FindRecordById("users", blockedID)` for each blocked user (N+1 pattern). Replace the per-block user fetch loop with a single query: collect all `blockedID` values, fetch users with `id IN (?)` in one query, then map the user data back. This reduces database round-trips from N+1 to 2.
- [ ] Add `session_rating` bounds validation to CreateSession and UpdateSession: in `api/handlers/sessions.go`, the `CreateSession()` and `UpdateSession()` handlers accept a `rating` field but don't validate its range. Add a check: if `rating` is provided and `rating < 1 || rating > 5`, return `400 { "error": "Rating must be between 1 and 5" }`. This matches the validation pattern used in `api/handlers/userbooks.go` for book ratings.
- [ ] Add `notes` max length validation to CreateSession and UpdateSession: in `api/handlers/sessions.go`, the `CreateSession()` and `UpdateSession()` handlers accept a `notes` field. The datamodel.md documents a 2000 char limit but this may not be enforced in the handler. Add a check: if `len(notes) > 2000`, return `400 { "error": "Notes must be 2,000 characters or fewer" }`.
- [ ] Add index on `activities(user, created DESC)` covering `activity_type` for feed filtering: in `api/handlers/activity.go`, the `GetFeed()` handler filters activities by `activity_type` when a `type` query param is provided, but the existing index on `(user_id, created_at DESC)` doesn't cover the type filter. Create a new PocketBase migration that adds a compound index on `(user, activity_type, created DESC)` to the `activities` collection. Follow the pattern in `api/migrations/1700000020_add_performance_indexes.go`.
- [ ] Add `genre_rating` bounds validation to SetGenreRatings: in `api/handlers/genreratings.go`, the `SetGenreRatings()` handler (around line 126) accepts rating values from the request and saves them to the `genre_ratings` collection without validating the range. The datamodel documents a 0-10 range, but neither the handler nor the migration enforces it (no CHECK constraint, no handler validation). Add a check in the loop: if `r.Rating < 0 || r.Rating > 10`, return `400 { "error": "Genre rating must be between 0 and 10" }`. This matches the validation pattern used in `api/handlers/userbooks.go` for book ratings.
- [ ] Return proper error responses instead of empty arrays in GetFeed on DB failure: in `api/handlers/activity.go`, the `GetFeed()` handler (around line 344) returns `200 { "activities": [], "next_cursor": null }` when the database query fails, masking errors from the frontend. Change this to return `500 { "error": "Failed to load feed" }` so the frontend can display an error state. Apply the same fix to `GetUserActivity()` (around line 411) which has the same pattern.
- [ ] Return proper error response in GetBookFollowerCount on DB failure: in `api/handlers/books.go`, the `GetBookFollowerCount()` function (around line 1525) returns `200 { "follower_count": 0 }` when the database query fails, masking errors. Change this to return `500 { "error": "Failed to count followers" }` so failures are visible in logs and debuggable.
- [ ] Clean up orphaned activities when deleting a shelf: in `api/handlers/collections.go`, the `DeleteShelf()` function (around line 187) deletes the collection record but does not clean up `activities` rows that reference the deleted collection via `collection_ref`. After the ownership check and before deleting the shelf, add a query to delete from `activities` where `collection_ref = shelfID` to prevent orphaned activity records that would cause broken links in user feeds.
- [ ] Validate `genre` name against predefined list in SetGenreRatings: in `api/handlers/genreratings.go`, the `SetGenreRatings()` handler accepts any string as the `genre` field when saving genre ratings. Add validation that the genre name is one of the 12 predefined genres: `Fiction`, `Non-fiction`, `Fantasy`, `Science fiction`, `Mystery`, `Romance`, `Horror`, `Thriller`, `Biography`, `History`, `Poetry`, `Children`. Return `400 { "error": "Unknown genre: <name>" }` for unrecognized genres. This prevents data pollution from typos or invalid values.
- [ ] Add `collection_name` max length validation to CreateShelf: in `api/handlers/collections.go`, the `CreateShelf()` handler validates that the name is non-empty but does not enforce a maximum length. The `collections.name` column is `varchar(255)`. Add a check: if `len(name) > 255`, return `400 { "error": "Shelf name must be 255 characters or fewer" }`. Apply the same check to `UpdateShelf()` when it accepts a name update. This provides a clear error instead of a database constraint error.
- [ ] Add `description` max length validation to CreateShelf and UpdateShelf: in `api/handlers/collections.go`, the `CreateShelf()` and `UpdateShelf()` handlers accept a `description` field. The datamodel documents a 1000 char max. Add a check: if `len(description) > 1000`, return `400 { "error": "Description must be 1,000 characters or fewer" }`. This matches the max length documented in `docs/documentation/datamodel.md`.

- [ ] Add file size validation to avatar and banner uploads: in `api/handlers/users.go`, the `UploadAvatar()` handler (line 850) and `UploadBanner()` handler (line 892) validate MIME type but do not check file size. A user can upload arbitrarily large files, which could exhaust disk space or memory. Add a size check before processing: read `e.Request.Header.Get("Content-Length")` and return `400 { "error": "Avatar must be 5 MB or smaller" }` if it exceeds 5 MB for avatars or `400 { "error": "Banner must be 10 MB or smaller" }` if it exceeds 10 MB for banners. As a belt-and-suspenders measure, also limit `e.Request` body size using `http.MaxBytesReader`. The banner migration already documents a 10 MB limit but the handler doesn't enforce it.
- [ ] Add `progress_pages` upper bound validation to UpdateBook: in `api/handlers/userbooks.go`, the `UpdateBook()` handler accepts `progress_pages` and validates it's `>= 0`, but does not check against the book's page count. If the user submits `progress_pages: 99999` it will be accepted even if the book has 300 pages. After the `>= 0` check, add: if `progress_pages > 0`, look up the user_books record's `device_total_pages` (or fall back to the book's `page_count`), and if a total is available and `progress_pages > total`, return `400 { "error": "Page number exceeds book length" }`. This prevents nonsensical progress values.
- [ ] Add `progress_percent` bounds validation to UpdateBook: in `api/handlers/userbooks.go`, the `UpdateBook()` handler accepts `progress_percent` without validating its range. Add a check: if `progress_percent` is provided and (`progress_percent < 0 || progress_percent > 100`), return `400 { "error": "Progress percent must be between 0 and 100" }`. The frontend already validates this client-side in `webapp/src/components/reading-progress.tsx` (line 96), but there's no server-side check.
- [ ] Add `bio` max length validation to UpdateProfile: in `api/handlers/users.go`, the `UpdateProfile()` handler (around line 818) accepts `bio` and `display_name` fields. The existing code checks `len(bio) > 2000` and `len(displayName) > 100`, but the datamodel documents `bio` as `text` with no explicit limit and `display_name` as `varchar(100)`. The `maxLength` in the frontend settings form is 500 for bio. Align the backend: change the bio length check from 2000 to 500 to match the frontend limit, or update the frontend `maxLength` to 2000 to match the backend. Either way, the limits should be consistent.

## API & Data Integrity

- [ ] Validate password confirmation matches password in Register: in `api/handlers/auth.go`, the `Register()` handler (around line 63-68) accepts both `password` and `passwordConfirm` fields in the request body, but never checks that they match. Add a check after the empty-field validation: if `data.Password != data.PasswordConfirm { return 400 "Passwords do not match" }`. The `ChangePassword` handler already validates password confirmation — apply the same pattern to registration for consistency.
- [ ] Enforce unique shelf names per user in CreateShelf and UpdateShelf: in `api/handlers/collections.go`, `CreateShelf()` (around line 100) does not check if a shelf with the same name already exists for the user. A user can create multiple shelves with identical names, which causes confusion in the UI (e.g., the shelf picker shows duplicate labels). Before saving, query: `FindRecordsByFilter("collections", "user = {:user} && name = {:name}")` and return `400 { "error": "A shelf with that name already exists" }` if found. Apply the same check to `UpdateShelf()` when the name is being changed (exclude the current shelf ID from the uniqueness check).
- [ ] Log errors from silent database queries in GetProfile: in `api/handlers/users.go`, the `GetProfile()` function (around lines 243-254) uses `_ = app.DB().NewQuery(...).One(&result)` for multiple stat queries (followers count, following count, friends count, etc.), silently discarding errors. Add `if err := ...; err != nil { app.Logger().Error("GetProfile: failed to count followers", "error", err) }` for each stat query so database errors are visible in logs for debugging. The stats should still fall back to zero values — just log the error instead of silently ignoring it.
- [ ] Include `review_comments` in account data deletion: in `api/handlers/userdata.go`, both `DeleteAccount` (line 23-46) and `DeleteAllData` (line 107-130) delete many user-owned collections but neither includes `review_comments`. Add `"review_comments"` to the `userOwned` slice in both handlers (after `"thread_comments"`). The `review_comments` collection has a `user` field and was added in a later migration — the deletion handlers were not updated to include it.
- [ ] Include `feedback` and `reports` in account data deletion: in `api/handlers/userdata.go`, both `DeleteAccount` and `DeleteAllData` delete many user-owned collections but are missing `feedback` and `reports`. Add `feedback` (where `user = userId`) and `reports` (where `reporter = userId`) to the `userOwned` slice in both handlers. Also add `review_likes` (where `user = userId`) which is similarly missing. This prevents orphaned user-contributed data from remaining after account deletion. The collections `book_quotes`, `reading_goals`, `saved_searches`, and `reading_sessions` are already included.
- [ ] Clean up orphaned activities when a user deletes their account: in `api/handlers/userdata.go`, the `DeleteAllData` handler deletes `activities` where `user = userId`, but `GetFeed` in `api/handlers/activity.go` queries activities from followed users — if user B deletes their account, user A's feed may still contain references to user B's activities. After deleting user B's activities, also delete any `activities` rows where `target_user = userId` to prevent broken "followed user" feed entries. This is a data consistency improvement.
- [ ] Validate `link_type` against allowed values in CreateBookLink: in `api/handlers/links.go`, the `CreateBookLink()` handler accepts a `link_type` field but does not validate it against the allowed values (`sequel`, `prequel`, `companion`, `mentioned_in`, `similar`, `adaptation`). Add a whitelist check: if the provided `link_type` is not in the allowed set, return `400 { "error": "Invalid link type. Must be one of: sequel, prequel, companion, mentioned_in, similar, adaptation" }`. Currently any string is accepted and stored, which could cause display issues on the frontend.
- [ ] Add `upper_bound` validation for pagination offset in GetAuthorDetail: in `api/handlers/books.go`, the `GetAuthorDetail()` handler validates that `offset >= 0` but does not enforce an upper bound. A request with `offset=999999999` would pass validation and be forwarded to Open Library, potentially causing timeouts or unexpected behavior. Add a check: if `offset > 10000`, return `400 { "error": "Offset must be 10,000 or fewer" }`. Apply the same pattern to `GetFollowedBooks` and `GetFollowedAuthors` which also accept unbounded offset parameters.
- [ ] Add `limit` upper bound to GetUserBooks: in `api/handlers/userbooks.go`, the `GetUserBooks()` handler defaults `limit` to 8 when it's 0 or negative but does not cap it at a maximum. A request with `limit=100000` would attempt to return all books in a single response. Add a check: if `limit > 100`, cap it to 100. This matches the max limit pattern used in `GetFollowedBooks` (max 50) and `GetBookEditions` (max 100) and prevents excessively large responses.
- [ ] Add `rating` bounds validation to UpdateShelfBook: in `api/handlers/collections.go`, the `UpdateShelfBook()` handler accepts a `rating` field and sets it on the `collection_items` record without validating its range. Add a check: if `rating` is provided and (`rating < 1 || rating > 5`), return `400 { "error": "Rating must be between 1 and 5" }`. This matches the validation pattern used in `api/handlers/userbooks.go` `UpdateBook()` for book ratings and prevents invalid data in the collection_items table.
- [ ] Log errors when local book search fails in SearchBooks: in `api/handlers/books.go`, the `SearchBooks()` handler runs a local catalog search alongside the Open Library search, but silently ignores errors from the local query (`localBooks, _ := ...`). Add `app.Logger().Error("local book search failed", "error", err)` when the local query fails so database issues are visible in logs. The search should still fall back gracefully to OL results, but the error should be logged for debugging. Apply the same logging to the `allStats, _ := ...` call that loads book stats.

## Profile & Social

- [ ] Fix FollowButton initial status on followers list page: on `webapp/src/app/[username]/followers/page.tsx`, each user in the followers list renders a `FollowButton` with `initialFollowStatus` hardcoded to `"none"` (line 136) instead of reflecting the actual follow relationship between the viewer and each listed user. To fix: have the server component fetch the viewer's following list via `GET /users/${viewerUsername}/following` (or use the already-available `is_following` field from the API response if the followers endpoint returns it), build a Set of followed user IDs, and pass the correct `initialFollowStatus` (`"active"`, `"pending"`, or `"none"`) to each `FollowButton`. This prevents the UI from showing "Follow" on users the viewer already follows.
- [ ] Show followed books on user profile sidebar: on `webapp/src/app/[username]/page.tsx`, add a "Followed Books" section below the "Followed Authors" sidebar section (after line 702). For the profile owner, fetch via `GET /me/followed-books?limit=5` (already exists). Show up to 5 book covers as small thumbnails with titles, and a "View all N →" link to `/settings/followed-books` when there are more than 5. This surfaces a feature that currently exists but is buried in settings.
- [ ] Show label/tag collections on visitor profile pages as a browsable sidebar: on `webapp/src/app/[username]/page.tsx`, the custom tag collections (line 272-274) and label keys (line 280) are computed but only shown as a count in the library summary link (line 481-495). Add a "Collections" section below the main content area that lists tag collections (name + count) and label keys (name + values count) as clickable links to `/${username}/tags/${slug}` and `/${username}/labels/${keySlug}/${valueSlug}` respectively, so visitors can browse the user's organizational system.
- [ ] Send notification when someone follows you: in `api/handlers/users.go`, the `FollowUser()` handler records a `followed_user` activity but does not create a notification for the followee. After the follow is created, insert a row into the `notifications` collection with `notif_type: "new_follower"`, `title: "{username} followed you"`, `metadata: { follower_username, follower_display_name }`, targeting the followee's user ID. Check `notification_preferences` for a future `new_follower` preference (default to sending). This matches the pattern used in `api/handlers/reviewlikes.go` for `review_liked` notifications.
- [ ] Add mutual followers count to the followers/following list pages: on `webapp/src/app/[username]/followers/page.tsx` and `webapp/src/app/[username]/following/page.tsx`, each user in the list shows avatar, name, and a follow button. Add a small "Mutuals: N" badge or "follows you" indicator next to users who follow the profile owner back. The `GET /users/:username` response already includes `is_following` — use this in the followers list to show which followers the profile owner follows back.
- [ ] Show total book count on user profile header: on `webapp/src/app/[username]/page.tsx`, the profile header shows followers/following counts and books-this-year but not the user's total book count across all statuses. The `GET /users/:username` response already includes book counts per status. Add a "N books" count to the stats row (sum of all status counts), matching the count shown on the `/users` browse page for each user card.
- [ ] Add reading timeline link to profile stats section: on `webapp/src/app/[username]/page.tsx`, the profile stats section shows books this year, average rating, and other metrics, but doesn't link to the timeline page at `/${username}/timeline`. Add a small "View timeline →" link below the stats section or as a clickable element on the "books this year" stat, so visitors can easily discover the timeline feature.
- [ ] Show "Follows you" badge on followers list page: on `webapp/src/app/[username]/followers/page.tsx`, each user in the followers list renders a `FollowButton` with `initialFollowStatus="none"` hardcoded instead of reflecting the actual follow relationship. Fetch the viewer's following list (or check `is_following` per user from the API response) and pass the correct `initialFollowStatus` to each `FollowButton`. Additionally, for the profile owner's view, show a "Follows you" badge next to followers that the owner follows back, matching the pattern already present on the following page.
- [ ] Add year-in-review link to profile stats section: on `webapp/src/app/[username]/page.tsx`, the profile stats section shows books this year and average rating but doesn't link to the year-in-review summary at `/${username}/year-in-review`. Add a small "Year in review →" link after the books-this-year stat that links to `/${username}/year-in-review?year=${currentYear}`. The year-in-review page already exists and shows rich stats — this link makes it discoverable from the profile.
- [ ] Show average rating distribution chart on user profile: on `webapp/src/app/[username]/page.tsx`, the profile stats show `average_rating` as a single number but don't show how the user's ratings are distributed. Add a small inline horizontal bar chart (5 bars for 1-5 stars, widths proportional to count) below the average rating stat. The `GET /users/:username` response already includes per-status book counts — add a `rating_distribution` field to the profile API response (a simple `SELECT rating, COUNT(*) FROM user_books WHERE user = :id AND rating > 0 GROUP BY rating`) and render it as a mini bar chart using `div` elements with dynamic widths.
- [ ] Show DNF count on user profile stats: on `webapp/src/app/[username]/page.tsx`, the profile stats row shows books this year, average rating, and total reviews, but does not show how many books the user has marked DNF. The `GET /users/:username` response includes `dnf_count` if the data is available from user_books status. Add a "N DNF" stat to the stats row, shown only when the count is > 0, matching the existing stat formatting.

- [ ] Add "Follows you" badge next to users on the following list page: on `webapp/src/app/[username]/following/page.tsx`, each user in the following list shows avatar, name, and follow button, but doesn't indicate which users follow the profile owner back. The `GET /users/:username/followers` response can be used to build a Set of follower IDs. For each user in the following list, show a small "Follows you" badge (matching `text-xs text-text-secondary`) next to users who also appear in the followers list. This helps users identify mutual follows at a glance.
- [ ] Add follow count to profile sidebar "Followed Authors" section: on `webapp/src/app/[username]/page.tsx`, the "Followed Authors" sidebar section shows author names and links but doesn't show the total count in the section header. Add the count to the header, e.g. "Followed Authors (12)", matching how the library summary shows book counts per status. The `followed_authors` array is already fetched — use its length for the profile owner view, or the total from the API response.

## Search & Browse

- [ ] Add book count next to genre name on genre detail page breadcrumb: on `webapp/src/app/genres/[slug]/page.tsx`, the page heading shows the genre name but the total book count is only shown in the pagination info. Add the total count next to the heading, e.g. "Fiction (142 books)", matching how the author page shows work count next to the author name.
- [ ] Add sort options to the genre detail page: on `webapp/src/app/genres/[slug]/page.tsx`, the API endpoint `GET /genres/:slug/books` already supports `sort=title|rating|year` query params (documented in `docs/documentation/api.md`), but the frontend page doesn't expose sort controls. Add a sort dropdown with options "Title (A-Z)", "Highest Rated", and "Newest" that updates the URL's `sort` query param, matching the pattern used on the users browse page (`webapp/src/app/users/page.tsx` sort dropdown).
- [ ] Add `aria-label` to genre detail page sort links: on `webapp/src/app/genres/[slug]/page.tsx`, if sort links are rendered as anchor tags or buttons, add `aria-label` attributes like `aria-label="Sort by title A to Z"`, `aria-label="Sort by highest rated"`, `aria-label="Sort by newest"` so screen readers announce the sort action. Also add `aria-current="true"` on the currently active sort option.
- [ ] Add genre page link to book detail page: on `webapp/src/app/books/[workId]/page.tsx`, the book's subjects are displayed as plain text chips. Make each subject chip a clickable link to `/genres/{slug}` (slugify the subject name), so users can browse other books in the same genre. Only link subjects that match the predefined genre list (Fiction, Fantasy, Science fiction, Mystery, Romance, Horror, Thriller, Biography, History, Poetry, Children, Non-fiction).
- [ ] Add pagination to genre index page: on `webapp/src/app/genres/page.tsx`, the genre list shows all genres as a card grid but has no pagination. If the number of genres grows, the page will become unwieldy. Add a simple "Show more" button or full pagination to the genre list, matching the pattern used on the users browse page.
- [ ] Add sort options to the author works grid: on `webapp/src/app/authors/[authorKey]/page.tsx`, the `AuthorWorksGrid` component shows works in the order returned from Open Library but provides no sort controls. Add a sort dropdown with options "Title (A-Z)", "Newest first", "Oldest first" that sorts the works client-side by `title` or `first_publish_year`. This matches the sort pattern used on genre detail pages and the users browse page.
- [ ] Add total count to search results pagination: on `webapp/src/app/search/page.tsx`, the Next page button visibility is determined by checking if results length `>= 20` (hardcoded). If the API returns exactly 20 results on the last page, a misleading "Next" button appears that leads to an empty page. Replace the `>= 20` check with the `total` field already returned by `GET /books/search` — show the Next button only when `page * 20 < total`. This eliminates the false "Next" button on the final page and enables showing "Page 2 of 5" in the pagination label.
- [ ] Add author search to user library search: in `webapp/src/components/library-manager.tsx`, the search field (around line 183-200) searches book titles within the user's library via `GET /users/:username/books/search?q=`. The API endpoint also searches `authors` via case-insensitive LIKE, but the search input placeholder only says "Search books" without indicating that author names also work. Update the placeholder to "Search books or authors" so users know they can search by author name within their library.
- [ ] Show "No results" message when library search returns empty: in `webapp/src/components/library-manager.tsx`, when the user types a search query and no books match, the book grid area becomes empty with no feedback. Add a `{searchQuery && books.length === 0 && !loading && <p className="text-sm text-text-secondary py-8 text-center">No books matching "{searchQuery}"</p>}` message in the grid area. This matches the empty-search pattern used on the `/search` page and `/users` browse page.

## Book Detail & Discovery

- [ ] Add notification when someone replies to your thread comment: in `api/handlers/threads.go`, the `AddComment()` handler sends `@mention` notifications via `fanOutMentionNotifications` but does NOT notify the parent comment's author when someone replies to their comment. When `data.ParentID` is set (reply to a comment), look up the parent comment's `user` field via `app.FindRecordById("thread_comments", data.ParentID)`. If the parent author is different from the current user, create a notification with `notif_type: "thread_reply"`, `title: "{username} replied to your comment in {thread_title}"`, `metadata: { thread_id, book_ol_id, thread_title }`. Check `ShouldNotify(app, parentAuthorID, "thread_reply")` before sending. The `thread_reply` preference field was already added to `notification_preferences` in migration `1700000019`.
- [ ] Show book cover thumbnails in notification items for book-related notifications: on `webapp/src/app/notifications/notification-list.tsx`, all notification items display as plain text rows. For notification types that reference a book (`book_new_thread`, `book_new_link`, `book_new_review`, `book_recommendation`), show a small 32x32px book cover thumbnail to the left of the notification text using the `metadata.book_cover_url` or `metadata.book_ol_id` to construct the cover URL. This makes notifications more visually scannable and matches the pattern used on the feed page where activity items include book covers.
- [ ] Add "Back to search" link on book detail page when arrived from search: on `webapp/src/app/books/[workId]/page.tsx`, there is no back-navigation to return to search results. Add a breadcrumb-style `← Back to search` link at the top of the page when the referrer is `/search`. Use `searchParams` to accept an optional `from=search&q=<query>` parameter, and when present, show a link back to `/search?q=<query>`. Update search result links in `webapp/src/app/search/page.tsx` to append `?from=search&q=${query}` to book detail hrefs.
- [ ] Show series position badge on book covers throughout the app: when a book has `series_position` data, display a small numbered badge (e.g., "#3") on the cover thumbnail in `BookCoverRow` (`webapp/src/components/book-cover-row.tsx`). The component already receives `series_position` in the book data (line 24 of `[username]/page.tsx` type definition). Add a small absolute-positioned badge in the top-right corner of the cover when `book.series_position` is set, using `text-xs bg-surface-2 border border-border rounded px-1`.
- [ ] Add breadcrumb navigation to author detail page: on `webapp/src/app/authors/[authorKey]/page.tsx`, there is no breadcrumb or back-navigation. Add a breadcrumb bar at the top of the page showing `Search > {Author Name}` (link "Search" to `/search?tab=authors`). This matches the breadcrumb pattern already used on thread detail pages (`webapp/src/app/books/[workId]/threads/[threadId]/page.tsx`).
- [ ] Add `aria-label` to BookFollowButton and AuthorFollowButton: in `webapp/src/components/book-follow-button.tsx`, the follow/unfollow button has no `aria-label`, so screen readers just read the generic button text. Add `aria-label={following ? "Unfollow this book" : "Follow this book"}`. Apply the same pattern to `webapp/src/components/author-follow-button.tsx` with `aria-label={following ? "Unfollow this author" : "Follow this author"}`.
- [ ] Add empty state to author works grid when author has no works: in `webapp/src/app/authors/[authorKey]/page.tsx`, when an author has zero works returned from Open Library, the `AuthorWorksGrid` component receives an empty array and renders nothing. Add an `EmptyState` message below the author bio like "No works found for this author on Open Library." using the existing `EmptyState` component pattern.
- [ ] Show error toast when BookFollowButton toggle fails: in `webapp/src/components/book-follow-button.tsx`, the follow/unfollow fetch call may fail silently without user feedback. After the fetch call, add an error check: if `!res.ok`, call `toast.error("Failed to update follow status")` and revert the optimistic state. Apply the same pattern to `webapp/src/components/author-follow-button.tsx`.
- [ ] Add review like count display on user reviews page: on `webapp/src/app/[username]/reviews/page.tsx`, each review card shows the book cover, title, rating, and review text, but doesn't show how many likes the review has received. The API `GET /users/:username/reviews` already returns `like_count` per review. Display a small heart icon with the count (e.g., "3") below the review text when `like_count > 0`.
- [ ] Show thread count on book detail page: on `webapp/src/app/books/[workId]/page.tsx`, the "Discussion" section header shows "Discussion" but not how many threads exist for the book. The `GET /books/:workId/threads` response includes `total`. Fetch this value and display it in the section header, e.g., "Discussion (5 threads)".
- [ ] Show recommendation count badge on nav bar: in `webapp/src/components/nav.tsx`, the notification bell shows an unread count badge, but there is no indicator for pending book recommendations. Fetch `GET /me/recommendations?status=pending` on the recommendations page (or add a lightweight `GET /me/recommendations/count` endpoint) and show a small badge on the "Recommendations" nav link when there are pending recommendations. This helps users discover they've received book recommendations.
- [ ] Add book cover to recommendation notifications: on `webapp/src/app/notifications/page.tsx`, book recommendation notifications show the sender name and book title as text, but no cover image. When `notif_type` is `book_recommendation` and `metadata.book_ol_id` is present, show a small book cover thumbnail next to the notification text using the book's `cover_url`. This makes recommendation notifications more visually appealing and recognizable.
- [ ] Add "Recommend to a friend" button on book detail page: on `webapp/src/app/books/[workId]/page.tsx`, the `POST /me/recommendations` endpoint exists for sending book recommendations, but there is no inline way to recommend a book from the book detail page. Add a "Recommend" button near the BookFollowButton that opens a small modal/dropdown with a username input (with autocomplete from followed users via `GET /users?q=`) and an optional note field. On submit, call `POST /api/me/recommendations`. This surfaces the recommendation feature which is currently only accessible from the `/recommendations` page.
- [ ] Show edition format badge on book covers when user has selected an edition: on `webapp/src/app/[username]/page.tsx` (profile page) and `webapp/src/components/shelf-book-grid.tsx` (label pages), when a book's `selected_edition_key` is set and the edition has a format (Hardcover, Paperback, eBook), display a small format badge in the bottom-left corner of the cover thumbnail. The user_books data already includes `selected_edition_key` — fetch format from the edition data or cache it on the `user_books` record. Use `text-xs bg-surface-2 border border-border rounded px-1` styling matching the series position badge pattern.
- [ ] Show date started on currently-reading book cards: on `webapp/src/app/[username]/page.tsx`, the "Currently Reading" spotlight section shows book covers with progress bars but doesn't display when the user started reading. The `date_started` field is already returned in the user_books query. Below each progress bar, add a small `text-xs text-muted` line showing "Started {date}" when `date_started` is set, formatted as a relative date (e.g., "Started 3 days ago") using the same date formatting pattern used in the activity sidebar.
- [ ] Add series detail page breadcrumb linking to book page: on `webapp/src/app/series/[seriesId]/page.tsx`, there is no breadcrumb or back-navigation. Add a breadcrumb bar at the top showing "Books > {Series Name}" (link "Books" to `/search`). When arrived from a book page (detect via `searchParams.from`), show "Books > {Book Title} > {Series Name}" with the book title linking back to the book detail page.
- [ ] Add report button to reviews on book detail page: on `webapp/src/app/books/[workId]/page.tsx`, community reviews are displayed but there is no way for users to report problematic reviews. The `POST /reports` endpoint already supports `content_type: "review"` and the `ReportModal` component exists (`webapp/src/components/report-modal.tsx`). Add a small "Report" option (e.g., via a three-dot menu or flag icon) on each review card that opens the `ReportModal` with `contentType="review"` and `contentId` set to the review's `user_id`. Apply the same pattern to thread comments on `webapp/src/app/books/[workId]/threads/[threadId]/page.tsx` using `content_type: "comment"`.
- [ ] Show reading sessions count on book detail page for owner: on `webapp/src/app/books/[workId]/page.tsx`, when the viewer owns this book and has reading sessions, show a small "Read N times" indicator near the book status section. The `GET /me/books/:olId/sessions` endpoint returns the session list — fetch it server-side and display the count. This surfaces re-read history without needing to scroll to the reading sessions section.
- [ ] Add notification type filter dropdown to notifications page: on `webapp/src/app/notifications/notification-list.tsx`, the notification list shows all notification types in a single chronological list with no filtering. Add a filter dropdown above the list with options "All", "New publications", "Book activity", "Social", "Recommendations" that filters the displayed notifications by `notif_type`. "New publications" = `new_publication`; "Book activity" = `book_new_thread`, `book_new_link`, `book_new_review`; "Social" = `new_follower`, `review_liked`, `thread_mention`, `thread_reply`; "Recommendations" = `book_recommendation`. Filter client-side from the already-fetched notifications list.
- [ ] Show spoiler warning count on book detail Discussion section: on `webapp/src/app/books/[workId]/page.tsx`, the Discussion section lists threads but doesn't indicate which threads contain spoilers until the user opens them. Add a small "(N contain spoilers)" note next to the thread count in the section header when any threads have `is_spoiler = true`. The `GET /books/:workId/threads` response already returns `is_spoiler` per thread — count them client-side and show the note.
- [ ] Add `aria-label` to edition expand/collapse button: on `webapp/src/app/books/[workId]/page.tsx`, the "Show all editions" / "Show fewer" toggle button in the editions section has no `aria-label`. Add `aria-label={expanded ? "Show fewer editions" : "Show all ${editionCount} editions"}` so screen readers announce the toggle action. Also add `aria-expanded={expanded}` to communicate the current state.

- [ ] Add error boundary for library compare page: the page at `webapp/src/app/library/compare/page.tsx` fetches label data server-side but has no `error.tsx` boundary. If the API is down or returns an unexpected error, the page crashes with a Next.js error screen. Create `webapp/src/app/library/compare/error.tsx` following the pattern in `webapp/src/app/books/[workId]/error.tsx` — show "Something went wrong" with a "Try again" button that calls `reset()`. This prevents a broken page when the API is temporarily unavailable.
- [ ] Add `aria-label` to ThreadLockToggle button: in `webapp/src/components/thread-lock-toggle.tsx`, the lock/unlock button has text content ("Lock" or "Unlock") but no `aria-label` attribute for screen readers. Add `aria-label={locked ? "Unlock this thread to allow new comments" : "Lock this thread to prevent new comments"}` so screen readers announce the full action, not just the button text.
- [ ] Show quoted page number as link context in BookQuoteList: in `webapp/src/components/book-quote-list.tsx`, when a quote has a `page_number` set, it's displayed as "p. {N}" but is not visually prominent. Style the page number as a small pill badge (e.g., `bg-surface-2 text-xs rounded px-1.5 py-0.5`) to make it more visually distinct from the quote text and note. This matches the badge styling used for format badges on editions.

## Settings & Account

- [ ] Add minimum password length validation to registration: in `api/handlers/auth.go`, the `Register()` handler checks that username, email, and password are non-empty but does NOT enforce a minimum password length. Add a check: if `len(password) < 8`, return `400 { "error": "Password must be at least 8 characters" }`. The `ChangePassword` handler at `api/handlers/auth.go` already has this check for password changes — apply the same rule to registration for consistency.
- [ ] Add `review_text` max length validation to UpdateBook and AddBook: in `api/handlers/userbooks.go`, both `AddBook()` and `UpdateBook()` accept a `review_text` field but never validate its length. Add a check: if `len(reviewText) > 10000`, return `400 { "error": "Review must be 10,000 characters or fewer" }`. This matches the 10,000 character limit enforced on thread body text in `api/handlers/threads.go`.
- [ ] Add character count display to bio and display_name fields in settings: in `webapp/src/components/settings-form.tsx`, the bio textarea has `maxLength={500}` and display_name has `maxLength={50}` but neither shows a character counter. Add a small `{current}/{max}` counter below each field (e.g., "142/500") that turns red when within 20 characters of the limit, matching the pattern used in thread body textarea.
- [ ] Show error toast when QuickAddButton fails: in `webapp/src/components/quick-add-button.tsx`, the `addWithStatus()` function shows a success toast on `res.ok` but silently does nothing on failure. Add an `else` branch that calls `toast.error("Failed to add book")` so the user knows the operation failed.
- [ ] Show error toast when email verification resend fails: in `webapp/src/components/email-verification-banner.tsx`, the resend handler silently ignores fetch failures. After the fetch call, add an error check: if `!res.ok`, call `toast.error("Failed to resend verification email")` to inform the user.
- [ ] Add notification preferences link to notifications page: on `webapp/src/app/notifications/page.tsx`, there is no link to the notification preferences section of settings. Add a small gear icon or "Preferences" link in the notifications page header that scrolls to or links to the notification preferences section on the settings page (`/settings#notifications`). This makes it easy for users to customize which notifications they receive without hunting through settings.
- [ ] Add `username` validation for length and reserved words in registration: in `api/handlers/auth.go`, the `Register()` handler validates that username is non-empty and matches `^[a-z0-9-]+$` but does not check for minimum/maximum length or reserved words. Add checks: username must be 3-30 characters, and must not be one of the reserved words: `admin`, `api`, `settings`, `login`, `register`, `search`, `feed`, `notifications`, `scan`, `library`, `books`, `authors`, `genres`, `series`, `users`, `feedback`, `recommendations`. Return `400 { "error": "Username must be 3-30 characters" }` or `400 { "error": "That username is reserved" }`.
- [ ] Add link to compare lists from library manager sidebar: in `webapp/src/components/library-manager.tsx`, the sidebar shows status counts, tags, and labels, but there is no link to the list comparison feature at `/library/compare`. Add a small "Compare lists" link at the bottom of the sidebar (below the labels section) pointing to `/library/compare`, matching the existing link on the settings page.
- [ ] Add export link to library manager sidebar: in `webapp/src/components/library-manager.tsx`, the sidebar has no link to CSV export. Add a "Export CSV" link at the bottom of the sidebar pointing to `/settings/export`. Users managing their library often want to export — this saves them a trip to settings.
- [ ] Add `review_comment` notification preference to settings UI: in `webapp/src/components/notification-preferences.tsx`, the toggle list includes preferences for new publications, threads, links, reviews, review likes, mentions, and recommendations, but does NOT include a toggle for `review_comment` notifications. The backend handler in `api/handlers/reviewcomments.go` checks `ShouldNotify(app, reviewUserID, "review_comment")`, but there is no `review_comment` field on the `notification_preferences` collection. Add: (1) a new `review_comment` boolean field (default true) to the `notification_preferences` collection via a new migration in `api/migrations/`, and (2) a toggle row in the `NotificationPreferences` component labeled "Comments on your reviews".
- [ ] Add `thread_reply` notification when someone replies to your thread comment: in `api/handlers/threads.go`, the `AddComment()` handler sends `@mention` notifications via `fanOutMentionNotifications` but does not notify the parent comment's author when someone replies to their comment. When `data.ParentID` is set (reply to a comment), look up the parent comment's `user` field and, if different from the current user, send a notification with `notif_type: "thread_reply"`, `title: "{username} replied to your comment"`, `metadata: { thread_id, book_ol_id, thread_title }`. Check `ShouldNotify` with a `thread_reply` preference (add field to `notification_preferences` if needed).
- [ ] Add `recommendation_note` max length validation to SendRecommendation: in `api/handlers/recommendations.go`, the `SendRecommendation()` handler accepts a `note` field but does not validate its length. The frontend `RecommendButton` component (`webapp/src/components/recommend-button.tsx` line 256) enforces `maxLength={2000}` on the textarea, but there's no server-side check. Add a handler-level check: if `len(note) > 2000`, return `400 { "error": "Note must be 2,000 characters or fewer" }`. This matches the max length pattern used for book link notes and thread comments.
- [ ] Add `tag_key_name` max length validation to CreateTagKey: in `api/handlers/tags.go`, the `CreateTagKey()` handler accepts a `name` field but does not enforce a maximum length. The `tag_keys.name` column is `varchar(100)`. Add a check: if `len(name) > 100`, return `400 { "error": "Label category name must be 100 characters or fewer" }`. This provides a clear application-level error instead of a database constraint error.
- [ ] Add `tag_value_name` max length validation to CreateTagValue: in `api/handlers/tags.go`, the `CreateTagValue()` handler accepts a `name` field but does not enforce a maximum length. The `tag_values.name` column is `varchar(100)`. Add a check: if `len(name) > 100`, return `400 { "error": "Label value name must be 100 characters or fewer" }`. This provides a clear error instead of a database constraint error.
- [ ] Add `feedback_title` and `feedback_description` max length validation to CreateFeedback: in `api/handlers/feedback.go`, the `CreateFeedback()` handler accepts `title` and `description` fields but does not enforce maximum lengths. Add checks: if `len(title) > 200`, return `400 { "error": "Title must be 200 characters or fewer" }`, and if `len(description) > 5000`, return `400 { "error": "Description must be 5,000 characters or fewer" }`. This prevents excessively long feedback submissions.
- [ ] Add `report_details` max length validation to CreateReport: in `api/handlers/reports.go`, the `CreateReport()` handler accepts a `details` field but does not enforce a maximum length. Add a check: if `len(details) > 2000`, return `400 { "error": "Details must be 2,000 characters or fewer" }`. This matches the max length pattern used for review comments and book quotes elsewhere.
- [ ] Add blocked user confirmation dialog to unblock action: in `webapp/src/app/settings/blocked/page.tsx`, the unblock button triggers the unblock action immediately with no confirmation. Wrap the unblock call in a `window.confirm("Unblock {username}? They will be able to see your profile and follow you again.")` check before proceeding. This matches the confirmation pattern used for book removal and account deletion elsewhere in the app.
- [ ] Add real-time username validation feedback on registration form: in `webapp/src/app/register/page.tsx`, the username input has a `pattern` attribute for validation but no real-time feedback when the user types invalid characters. Add an `onChange` handler that checks the value against `^[a-z0-9-]+$` and shows an inline error message (e.g., "Only lowercase letters, numbers, and hyphens allowed") below the input when invalid characters are typed. Also show a green checkmark or "Available" indicator when the username matches the pattern and is at least 3 characters. This prevents confusion from submitting and getting a server error.
- [ ] Add password strength indicator to registration form: in `webapp/src/app/register/page.tsx`, the password input has `minLength={8}` but no visual indicator of password requirements. Add a small requirements checklist below the password field that checks off "At least 8 characters" in real-time as the user types. Use `text-xs` with green checkmark for met and gray for unmet, matching the character counter pattern used in the thread body textarea.
- [ ] Show error toast when reading session save fails: in `webapp/src/components/reading-history.tsx`, the create/update session handlers may fail silently. After the `POST /api/me/books/:olId/sessions` and `PATCH /api/me/sessions/:sessionId` calls, add an error check: if `!res.ok`, call `toast.error("Failed to save reading session")`. Apply the same pattern to the delete handler with `toast.error("Failed to delete reading session")`. This matches the error toast pattern used in BookFollowButton and other interactive components.
- [ ] Add confirmation dialog before deleting a reading session: in `webapp/src/components/reading-history.tsx`, the delete button for reading sessions triggers immediate deletion with no confirmation. Wrap the delete call in a `window.confirm("Delete this reading session? This cannot be undone.")` check before calling `DELETE /api/me/sessions/:sessionId`. This matches the confirmation pattern used for book removal in the library manager and account deletion in settings.
- [ ] Add reading goal management page at `/settings/goals`: the `PUT /me/goals/:year` endpoint exists for creating/updating reading goals, and the `ReadingGoalCard` component displays progress on the profile page, but there is no dedicated settings page for managing goals. Create a new page at `webapp/src/app/settings/goals/page.tsx` that: (1) shows the current year's goal with a numeric input to set/update the target, (2) lists all past goals with their progress (via `GET /me/goals`), and (3) has a save button that calls `PUT /api/me/goals/${year}`. Add a "Reading goals" link to the settings page sidebar. Create a corresponding proxy route at `webapp/src/app/api/me/goals/route.ts` if one doesn't exist.
- [ ] Show error toast when genre rating save fails: in `webapp/src/components/genre-rating-editor.tsx`, the save handler for genre ratings may fail silently if the `PUT /api/me/books/:olId/genre-ratings` call returns an error. After the fetch call, add an error check: if `!res.ok`, call `toast.error("Failed to save genre ratings")` and revert the optimistic state. This matches the error toast pattern used in BookFollowButton and other interactive components.
- [ ] Add "Clear all" button to genre rating editor: in `webapp/src/components/genre-rating-editor.tsx`, when a user has set genre ratings on a book, there is no way to clear all ratings at once. Add a "Clear all ratings" link below the sliders that sends `PUT /api/me/books/:olId/genre-ratings` with all genres set to 0 (which deletes the ratings). This is useful when a user wants to retract all genre votes on a book.
- [ ] Show error toast when block/unblock action fails: in `webapp/src/app/[username]/page.tsx`, the block/unblock button triggers a fetch to `POST /api/users/:username/block` or `DELETE /api/users/:username/block`, but if the request fails, there is no error feedback to the user. Add an error check after the fetch: if `!res.ok`, call `toast.error("Failed to block user")` or `toast.error("Failed to unblock user")` and revert any optimistic state. This matches the error toast pattern used in FollowButton and BookFollowButton.
- [ ] Add `aria-label` to ShelfPicker status option buttons: in `webapp/src/components/shelf-picker.tsx`, the status buttons inside the dropdown (Want to Read, Currently Reading, Finished, DNF, etc.) have no `aria-label` attributes. Add `aria-label={`Set status to ${statusName}`}` on each status option button so screen readers clearly announce what each button does. Also add `aria-label="Remove from library"` to the remove button. This matches the aria-label pattern applied to sort options and follow buttons elsewhere in the app.

- [ ] Show error toast when reading goal save fails: in `webapp/src/components/reading-goal-form.tsx`, the save handler for reading goals may fail silently if the `PUT /api/me/goals/:year` call returns an error. After the fetch call, add an error check: if `!res.ok`, call `toast.error("Failed to save reading goal")`. Import `useToast` if not already imported. This matches the error toast pattern used in FollowButton and BookFollowButton.
- [ ] Add `email` format validation to UpdateEmail: in `api/handlers/auth.go`, the `ChangeEmail()` handler validates that the new email is non-empty and different from the current email, but does not validate the email format beyond checking for `@`. Add a proper email format validation using `mail.ParseAddress(email)` from Go's `net/mail` package. Return `400 { "error": "Invalid email address" }` on failure. This prevents malformed email addresses from being stored.
- [ ] Add `display_name` and `bio` trimming in UpdateProfile: in `api/handlers/users.go`, the `UpdateProfile()` handler sets `display_name` and `bio` directly from the request without trimming whitespace. Add `strings.TrimSpace()` to both fields before length validation and before saving, so leading/trailing whitespace doesn't count against the character limit and isn't stored in the database. Apply the same trimming to `username` in `Register()`.
- [ ] Add Goodreads import count to activity feed: when a user completes a Goodreads import via `POST /me/import/goodreads/commit`, no activity is recorded. After a successful import, create an activity with `activity_type: "imported_books"` and `metadata: { count: N, source: "goodreads" }` where N is the number of books successfully imported. Display on the feed as "{user} imported {N} books from Goodreads". This makes large imports visible in the social feed. Apply the same pattern to StoryGraph and LibraryThing imports.
- [ ] Add `me/goals` proxy route if missing: the `ReadingGoalForm` component on the settings page calls `PUT /api/me/goals/:year`, but verify that a proxy route exists at `webapp/src/app/api/me/goals/[year]/route.ts` with a PUT handler that forwards to the Go API. If missing, create the proxy route following the pattern in `webapp/src/app/api/me/password/route.ts`. Also create `webapp/src/app/api/me/goals/route.ts` with a GET handler for listing all goals.
- [ ] Add settings nav pill for "Goals": in `webapp/src/components/settings-nav.tsx`, the settings navigation pills include Profile, Import, Export, Labels, API Tokens, Ghost Activity, Follow Requests, Feedback, Blocked, Followed Books, and Followed Authors — but there is no pill for "Goals" even though the reading goal form exists on the settings page. Add a "Goals" pill linking to `/settings/goals` (or to the goals section on the settings page). Place it after "Export" since reading goals are a core user feature.

## Documentation

- [ ] Add `recommendations` collection to datamodel.md: the `recommendations` collection exists in migration `1700000013` and is fully implemented in `api/handlers/recommendations.go` with fields `sender`, `recipient`, `book`, `note`, `status` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections, placed after the `notifications` section.
- [ ] Add `reading_goals` collection to datamodel.md: the `reading_goals` collection exists in migration `1700000014` and is fully implemented in `api/handlers/goals.go` with fields `user`, `year`, `target`, `current` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections.
- [ ] Add `saved_searches` and `reading_sessions` collections to datamodel.md: both collections exist in migration `1700000017` files and are fully implemented in handlers but have no entries in `docs/documentation/datamodel.md`. Add table definitions for both: `saved_searches` (fields: `user`, `query`, `filters`) and `reading_sessions` (fields: `user`, `book`, `started_at`, `finished_at`, `pages_read`, `notes`).
- [ ] Update "Planned tables" section in datamodel.md to reflect implementation status: in `docs/documentation/datamodel.md`, the "Planned tables" section lists `authors`/`book_authors`, `reviews`, and `book_edits` as unimplemented. Add a note that `reviews` functionality is integrated into `user_books` (rating + review_text fields) and `authors` are stored as a TEXT field on `books`. Mark `book_edits` as still planned.
- [ ] Add `email_verification_tokens` collection to datamodel.md: the `email_verification_tokens` collection exists in migration `1700000016` and is used by `api/handlers/auth.go` for email verification (fields: `user`, `token_hash`, `expires_at`, `used`), but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of `password_reset_tokens`, placed after it.
- [ ] Add `series` and `book_series` API endpoints to api.md: the series endpoints (`GET /series/search`, `GET/POST /books/:workId/series`, `GET/PATCH/DELETE /series/:seriesId`, `GET /authors/:authorKey/series`) are fully implemented and documented in the OpenAPI spec, but `docs/documentation/api.md` already has a Series section. Verify completeness of the api.md Series section and ensure `PATCH /series/:seriesId` and `DELETE /series/:seriesId` are documented. Add `GET /authors/:authorKey/series` if missing.
- [ ] Add `feedback` API endpoints to api.md: the feedback endpoints (`GET /me/feedback`, `POST /me/feedback`, `DELETE /me/feedback/:feedbackId`) are fully implemented in `api/handlers/feedback.go` and registered in `api/main.go`, but have no entries in `docs/documentation/api.md`. Add a "Feedback" section with request/response documentation for all three endpoints, including the `type` (bug/feature), `title`, `description`, `steps_to_reproduce`, and `severity` fields. Place it after the Notifications section.
- [ ] Add `reports` API endpoints to api.md: the reports endpoints (`POST /me/reports`, `GET /admin/reports`, `PUT /admin/reports/:reportId`) are fully implemented in `api/handlers/reports.go` and registered in `api/main.go`, but have no entries in `docs/documentation/api.md`. Add a "Reports" section documenting the create endpoint (content_type, content_id, reason, details fields) and the admin review endpoints. Place it after the Feedback section.
- [ ] Add `blocks` collection to datamodel.md: the `blocks` collection exists in migration `1700000015` and is fully implemented in `api/handlers/blocks.go` with fields `blocker`, `blocked`, `created` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections, placed after the `follows` section. Include the unique constraint on `(blocker, blocked)` and the index on `blocked`.
- [ ] Add `user_theme` collection to datamodel.md: the `user_theme` collection exists in migration `1700000021` and is used by theme endpoints in `api/handlers/` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections.
- [ ] Add `reading_goals` API endpoints to api.md: the reading goals endpoints (`GET /me/goals`, `PUT /me/goals/:year`) are fully implemented in `api/handlers/goals.go` and registered in `api/main.go`, but have no entries in `docs/documentation/api.md`. Add a "Reading Goals" section documenting both endpoints: the GET returns all goals (year, target, current), the PUT creates/updates a goal for a given year with a `target` number field. Place it after the Reading Sessions section.
- [ ] Add `book_quotes` API endpoints to api.md: the book quotes endpoints (`GET /books/:workId/quotes`, `GET /me/books/:olId/quotes`, `POST /me/books/:olId/quotes`, `DELETE /me/quotes/:quoteId`) are fully implemented in `api/handlers/quotes.go` and registered in `api/main.go`, but have no entries in `docs/documentation/api.md`. Add a "Book Quotes" section documenting all four endpoints, including the `text` (max 2000), `page_number`, `note` (max 500), and `is_public` fields. Place it after the Genre Ratings section.
- [ ] Add `api_tokens` API endpoints to api.md: the API token endpoints (`GET /me/api-tokens`, `POST /me/api-tokens`, `DELETE /me/api-tokens/:tokenId`) and the Bearer token middleware are fully documented in `docs/documentation/api.md` already. Verify the api.md documentation is complete and accurate — specifically, check that the token auth middleware behavior (reading `Authorization: Bearer` header, looking up SHA-256 hash in `api_tokens` table) is documented. If missing, add a section explaining how external clients authenticate with API tokens vs. JWT cookies.
- [ ] Add `computed_collections` collection to datamodel.md: the `computed_collections` collection exists in migration `1700000018` and is used by set-operation and cross-user-compare features (see `docs/planning/completed.md` "Continuous Lists" section) with fields `collection_id` (FK to collections), `operation`, `source_a`, `source_b`, `cross_username`, `cross_slug`, `is_continuous`, `last_computed_at` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections, placed after the `collections` section.
- [ ] Add `review_likes` collection to datamodel.md: the `review_likes` collection exists in a migration and is used by `api/handlers/reviewlikes.go` with fields `user`, `book`, `review_user` (the user whose review was liked) and a unique index on `(user, book, review_user)` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections, placed after the `user_books` section.
- [ ] Add `review_comments` collection to datamodel.md: the `review_comments` collection exists in migration and is used by `api/handlers/reviewcomments.go` with fields `user`, `book`, `review_user`, `body`, `deleted_at` and indexes on `(book, review_user, deleted_at)` and `user` — but has no entry in `docs/documentation/datamodel.md`. Add a table definition following the format of existing collections, placed after the `review_likes` section.
- [ ] Consolidate duplicate "Relationships" section in datamodel.md: in `docs/documentation/datamodel.md`, there are two separate "Relationships" sections — one around line 255-269 and another more complete one around line 398-428. The earlier section appears to be an incomplete duplicate. Remove the earlier section (lines 255-269) and keep only the more comprehensive later section, ensuring no unique content is lost.
